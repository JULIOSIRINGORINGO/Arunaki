- Fetches content from a specified URL using a simple HTTP request (no JavaScript execution)
- Takes a URL and optional format as input
- Fetches the URL content, converts to requested format (markdown by default)
- Returns the content in the specified format
- Use this tool ONLY for static pages like documentation, APIs, or simple text content

Usage notes:
  - IMPORTANT: For e-commerce sites, product pages, stock checks, or any site that uses JavaScript to render content, use the browse_website tool instead. This tool (webfetch) CANNOT see JavaScript-rendered content, modals, stock tables, or data behind buttons.
  - IMPORTANT: if another tool is present that offers better web fetching capabilities, is more targeted to the task, or has fewer restrictions, prefer using that tool instead of this one.
  - DO NOT use this tool for: cititex.com, tokopedia.com, shopee.co.id, bukalapak.com, or any e-commerce/SPA site. Use browse_website instead.
  - The URL must be a fully-formed valid URL
  - HTTP URLs will be automatically upgraded to HTTPS
  - Format options: "markdown" (default), "text", or "html"
  - This tool is read-only and does not modify any files
  - Results may be summarized if the content is very large
